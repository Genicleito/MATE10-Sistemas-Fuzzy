source('funcoesPertinenciaFuzzy.r')

# Variável Temperatura
termoTempBaixa <- seq(1, 6, seq = 1)
termoTempAlta <- seq(3, 8, seq = 1)

# Variável Umidade
termoUmidBaixa <- seq(0, 5, seq = 1)
termoUmidAlta <- seq(2, 8, seq = 1)